version: "3.9"
services:
  lernplan-reminder-bot-v2:
    build: .
    container_name: lernplan-reminder-bot-v2
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    # Optional: Logs nach außen
    # ports: []  # Telegram nutzt ausgehend HTTPS, kein Port Mapping nötig
